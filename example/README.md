# Example app

Components need an app that uses them in order to run codegen. This example
app is also useful for testing and documentation.

Run the example and `convex` commands from the root of the repo:

```sh
npm i
npm run dev
```
